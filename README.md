ポートを指定してプログラムを実行
```
cargo run 7878
```


チェーンにブロックを追加
```
curl -X POST http://127.0.0.1:7878/add_block

```

別コンピュータのチェーンのポートを登録
```
curl -X POST -d "127.0.0.1:7879" http://127.0.0.1:7878/register
```


登録したポートのチェーンと同期
```
curl -X GET http://127.0.0.1:7878/resolve
```